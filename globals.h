#ifndef GLOBALS_H
#define GLOBALS_H

// ------------------------------------------------------------------------- //

#include <math.h>
#include <stddef.h>

static const char kNew_line        = '\n';
static const char *kShade_chars    = ".,-~:;=!*#$@";
static const float kTwo_pi         = 2 * M_PI;
static const float kPhi_step       = 0.07;
static const float kTheta_step     = 0.02;
static const size_t kCanvas_height = 22;
static const size_t kCanvas_width  = 80;
static const size_t kCanvas_size   = 22 * 80;

// ------------------------------------------------------------------------- //

#endif
